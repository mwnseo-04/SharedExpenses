[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so trip routes/refresh work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
