services:
  - type: web
    name: sharedexpenses-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120 wsgi:app
    envVars:
      - key: ACCESS_PIN
        generateValue: false
        value: "48291"
      - key: PYTHON_VERSION
        value: "3.12.10"
